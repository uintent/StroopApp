<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:keepScreenOn="true"
    tools:context=".StroopDisplayActivity">

    <!-- Main Stroop Display Container -->
    <FrameLayout
        android:id="@+id/stroop_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="@dimen/stroop_margin_horizontal"
        android:layout_marginEnd="@dimen/stroop_margin_horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Countdown Display -->
        <TextView
            android:id="@+id/tv_countdown"
            style="@style/CountdownTextStyle"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/black"
            android:gravity="center"
            android:textColor="@color/white"
            android:visibility="gone"
            tools:text="3"
            tools:visibility="visible" />

        <!-- Stroop Stimulus Display -->
        <TextView
            android:id="@+id/tv_stroop_stimulus"
            style="@style/StroopTextStyle"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:gravity="center"
            android:visibility="gone"
            tools:text="红"
            tools:textColor="@color/stroop_blue"
            tools:visibility="visible" />

        <!-- Interval Display (White background, no text) -->
        <View
            android:id="@+id/view_interval"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:visibility="gone" />

        <!-- Black Screen (Default state) -->
        <View
            android:id="@+id/view_black_screen"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/black"
            android:visibility="visible" />

    </FrameLayout>

    <!-- Hidden moderator controls for emergency situations -->
    <!-- These are invisible but can be activated by touch for emergency stop -->
    <LinearLayout
        android:id="@+id/layout_hidden_controls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:alpha="0.0"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Emergency Stop Button (invisible but functional) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_emergency_stop"
            android:layout_width="100dp"
            android:layout_height="60dp"
            android:text="STOP"
            android:textColor="@color/white"
            app:backgroundTint="@color/error" />

    </LinearLayout>

    <!-- Status indicator for moderator (very subtle) -->
    <TextView
        android:id="@+id/tv_status_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:alpha="0.3"
        android:background="@color/black"
        android:padding="4dp"
        android:text="●"
        android:textColor="@color/primary"
        android:textSize="12sp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>